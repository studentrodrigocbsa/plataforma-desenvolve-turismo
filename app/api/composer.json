{
    "name": "rcbsa/surveycapacitismo",
    "description": "TCC Rodrigo Bittencourt",
    "type": "project",
    "autoload": {
        "psr-4": {
            "": ["src/"]
        }
    },
    "require-dev": {
        "phpstan/phpstan": "^2.1"
    },
    "require": {
        "phputil/router": "^0.3.3",
        "phputil/cors": "^0.5.2"
    },
    "scripts": {
        "check": "phpstan analyse src",
        "server": [
            "Composer\\Config::disableProcessTimeout",
            "php -S localhost:8080"
        ]
    }
}
